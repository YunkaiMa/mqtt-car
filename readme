# STM32 MQTT 自主移动机器人网页控制端 README

## 1. 项目简介

这是一个纯前端的网页控制台，用于通过 MQTT 协议远程控制基于 STM32 + ESP-01M 的 MQTT 自主移动机器人。  
网页使用 **HTML + CSS + JavaScript** 编写，依赖 **mqtt.js** 库，通过 **WebSocket** 连接 MQTT Broker，向自主移动机器人订阅的 Topic 发布 JSON 指令，并实时显示自主移动机器人上报的编码器状态和通信日志。

无需后端服务器，可本地打开，也可一键发布到 **GitHub Pages**，手机和电脑浏览器均可访问。

## 2. 功能特性

- **MQTT over WebSocket 连接**：支持配置 Broker 地址、端口、路径、Client ID、用户名/密码。
- **图形化方向控制**：圆形方向盘按钮，支持前进、后退、左转、右转、停止、原地旋转 180°。
- **参数可调**：距离（cm）或角度（°）可输入，速度通过滑块调节（30%–100%）。
- **快捷预设**：一键填入 10/30/50/100 cm、90°/180°。
- **实时状态显示**：显示左右编码器脉冲数、忙标志、收到消息计数。
- **通信日志**：完整记录发送和接收的 MQTT 消息，便于调试。
- **键盘控制**：支持 W/A/S/D、方向键、空格键快捷操作。
- **响应式设计**：适配手机和电脑屏幕，触摸友好。
- **本地记忆**：自动保存上次填写的 Broker 配置（localStorage）。

## 3. 文件说明

| 文件 | 说明 |
|------|------|
| `index.html` | 网页控制端全部代码（HTML + CSS + JS 内联） |
| `README.md` | 本说明文档 |

> 网页依赖 `mqtt.js`，默认从 CDN 加载：  
> `https://unpkg.com/mqtt@5.10.1/dist/mqtt.min.js`  
> 如需离线使用，可下载该文件到本地，并修改 HTML 中的 `<script src="...">` 路径。

## 4. 快速开始

### 4.1 本地运行（推荐用于调试）

浏览器出于安全限制，禁止 `file://` 协议页面发起 WebSocket 连接，因此**不能直接双击 HTML 文件**，需要启动一个本地 HTTP 服务器。

**使用 Python（最简单）**：

```bash
# 进入 index.html 所在目录
cd /path/to/your/folder

# Python 3
python -m http.server 8000
